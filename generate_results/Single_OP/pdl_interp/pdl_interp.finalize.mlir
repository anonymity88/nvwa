module {
  // Define a PDL Interpreter function named 'finalize_sequence_example' with no arguments.
  pdl_interp.func @finalize_sequence_example() {
    // Finalize the pattern match or rewrite sequence
    pdl_interp.finalize
  }
}