Assume you are a senior compiler engineer specializing in the MLIR infrastructure and ODS. Your task is to generate an ATOMIC MLIR IR snippet that targets EXACTLY ONE specific operator based on its official specification.

### Task Objective:
Analyze the provided definition of the "$AAA$" operator and generate a minimal test case.
1. **Singularity Rule:** The generated `func.func` must contain only ONE instance of the "$AAA$" operator as its core logic. Do not include any other functional operators unless they are strictly required to provide valid inputs (e.g., `tosa.const` for tensor initialization).
2. **Syntactic Rules:** The exact mnemonic, operand count, and result count. 
3. **Attribute Constraints:** Required and optional attributes (e.g., padding, strides, dilations) and their expected types. 
4. **Type Invariants:** Tensor ranks, element types (e.g., f32, i32), and shape propagation rules defined in the ODS.

### Strict Technical Requirements:
- **ODS Adherence:** The generated IR must strictly satisfy all constraints defined in the operator's specification to ensure it passes the mlir-opt verification. 
- **Syntactic Correctness:** Use func.func @name instead of a bare func. 
- **Global Definitions:** All affine_map, attribute alias, or type alias definitions must be placed at the top-level, strictly before the module {} block. 
- **Undefined Behavior (UB) Avoidance:** Ensure tensor dimensions and indexing are consistent to prevent any illegal memory access or logical errors.

### Output Format:
Your response must contain the complete MLIR code for an atomic module enclosed within ```mlir and ```.
Expected structure:
```mlir
// Global definitions here
module {
    func.func @main(%arg0: tensor<...>) -> tensor<...> {
        // Core target operator only
        %0 = "dialect.op_name"(%arg0) { ... } : (tensor<...>) -> tensor<...>
        return %0 : tensor<...>
    }
}

Few-shot Examples (Atomic Generation):
Example 1- 
Please generate a piece of IR code based on the description and definition of the $OP1$ operator provided in the following official documentation:
$OPdefine1$
The generated code is as follows:
$result1$

Example 2- 
Please generate a piece of IR code based on the description and definition of the $OP2$ operator provided in the following official documentation:
$OPdefine2$
The generated code is as follows:
$result2$

Example 3- 
Please generate a piece of IR code based on the description and definition of the $OP3$ operator provided in the following official documentation:
$OPdefine3$
The generated code is as follows:
$result3$

Target -
Generate an ATOMIC MLIR IR snippet for the "$AAA$" operator based on the specification below. Ensure that the generated code contains no more than one instance of "$AAA$".
Target Specification: 
@@@@
Note: Prioritize the correctness of the "$AAA$" operator's syntax. Keep the context minimal and focused entirely on this single operation.